apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: heartbeat
  namespace: test-workloads
spec:
  workspaces:
    - name: cluster
  params:
    - name: PROW_JOB_ID
      type: string
  tasks:
    - name: deploy
      taskRef:
        name: opsctl-deploy
      workspaces:
        - name: cluster
          workspace: cluster
